package ${packag}.service;

import ${packag}.entity.${className}Entity;
import ${packag}.dos.${className}Do;
import cn.t200.quickboot.data.mybatisplus.IBaseService2;

/**
 * <p>
 * ${tableComment!} 服务类
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */

public interface I${className}Service extends IBaseService2<${className}Entity, ${className}Do> {

}
